<!DOCTYPE html>
@* AÑADIDO: lang="es" para accesibilidad *@
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Mercado Pago App</title>

    @* *** CARGA DIRECTA DE CSS (SIN BUNDLING) *** *@
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />

    @* *** Incluir Font Awesome (Íconos) *** *@
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" xintegrity="sha512-SnH5WK+bZxgPHs44uWIX+LLMD/CDQ65zR...[EL RESTO DE INTEGRITY]..." crossorigin="anonymous" referrerpolicy="no-referrer" />

    @* ELIMINADO: modernizr.js ya no es necesario con navegadores modernos *@
</head>
@* AÑADIDO: class="bg-light"
    Esto le da a toda la app el fondo gris claro que empezamos
    a usar en la billetera, dándole un look más cohesivo.
*@
<body class="bg-light">
    @* Navbar con Enlaces Personalizados y Color (BG-Primary depende de tu CSS) *@
    <div class="navbar navbar-expand-sm navbar-dark bg-dark fixed-top shadow-sm">
        <div class="container">

            @* 1. Marca de la Aplicación (navbar-brand) *@
            @Html.ActionLink("Mercado Pago", "Index", "Billetera", new { area = "" }, new { @class = "navbar-brand" })

            @* 2. Botón Hamburguesa de Bootstrap 5 (Requiere data-bs-toggle y data-bs-target) *@
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            @* 3. El colapsable ahora usa id="navbarNav" *@
            <div class="collapse navbar-collapse" id="navbarNav">
                @* AÑADIDO: "ms-auto" (margin-start: auto) para alinear los enlaces a la derecha *@
                <ul class="navbar-nav ms-auto">
                    @* AÑADIDO: Lógica para marcar el enlace activo (opcional pero recomendado).
                        Esto requiere que definas ViewBag.ActiveLink en tus controladores.
                        Ej: ViewBag.ActiveLink = "Billetera";
                    *@
                    <li class="nav-item">
                        @Html.ActionLink("Mi Billetera", "Index", "Billetera", null, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Transacciones", "Index", "Transaccion", null, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Usuarios (Admin)", "Index", "Usuario", null, new { @class = "nav-link" })
                    </li>
                </ul>
            </div>
        </div>
    </div>

    @* AÑADIDO: tag <main> para semántica HTML5
        AÑADIDO: "pt-3" (padding-top) para más espacio bajo la navbar
    *@
    <main role="main" class="container body-content mt-5 pt-3">
        @RenderBody()
    </main>

    @* ELIMINADO: El <hr /> y <footer> simple *@

    @* AÑADIDO: Un footer oscuro que coincide con la navbar *@
    <footer class="bg-dark text-white text-center p-4 mt-5">
        <div class="container">
            <p class="mb-0">&copy; @DateTime.Now.Year - Aplicación Distribuida</p>
        </div>
    </footer>


    @* *** CARGA DIRECTA DE SCRIPTS (CORREGIDO) *** *@

    @* CORREGIDO:
        Cambiado de jquery-1.10.2.min.js a jquery-3.7.1.min.js (el que instalaste).
        Esto es VITAL para que las validaciones funcionen.
    *@
    <script src="~/Scripts/jquery-3.7.1.min.js"></script>

    @* CORREGIDO:
        Cambiado de bootstrap.min.js (BS4) a bootstrap.bundle.min.js (BS5).
        Esto es VITAL para que el menú hamburguesa funcione.
    *@
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>

    @* AÑADIDO: Colocado el RenderSection AL FINAL, que es la mejor práctica. *@
    @RenderSection("Scripts", required: false)
</body>
</html>